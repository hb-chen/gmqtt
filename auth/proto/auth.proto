syntax = "proto3";

package proto;

enum BASE_PATH {
    rpc = 0;
}

enum SRV {
    Auth = 0;
}

enum METHOD {
    Verify = 0;
}

message Req {
    string name = 1;
    string pwd = 2;

}

message Resp {
    string token = 1;
    bool verified = 2;
}